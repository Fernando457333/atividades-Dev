const nomeEstudantes = ['jose', 'joao', 'maria', 'joana', 'andre'];

nomeEstudantes[0] = 'andreia';//assim podendo trocar de forma eficaz

console.log(nomeEstudantes);

//podendo usar outro exemplo 

const x = 3;
nomeEstudantes[x] = 'andreia';