const numeros = [0, 5, 3, 69, 45, 18, 13, 2]

let soma = 0

for (let i = o; i < numeros.length; i++) {
    //sera execultado para i de 0 ate 7  [ 8 nao sera execultado
    soma = soma + numeros[i]
    //soma += numeros[i]
}

console.log(soma);